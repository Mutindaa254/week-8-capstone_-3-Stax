        # Dependencies
        /node_modules
        /.pnpm-store # If using pnpm, ignore its global store artifacts within the project
        /pnpm-lock.yaml # You might want to commit this for exact dependency versions, but often ignored in simpler setups
        
        # Environment variables
        .env
        .env.local
        .env.*.local
        
        # Build artifacts
        /dist
        /build
        
        # Logs
        npm-debug.log*
        yarn-debug.log*
        yarn-error.log*
        .pnpm-debug.log*
        
        # OS generated files
        .DS_Store
        .idea/
        .vscode/
        *.iml
        